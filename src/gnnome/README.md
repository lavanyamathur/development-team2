# gnnome

GNN-based edge-scoring model, plus the full / half / quarter width variants.

TODO:
- Port/vendor GNNome's model code (https://github.com/lvrcek/GNNome)
- Add width-reduction logic driven by configs/model_variants.yaml
- (Bonus) depth-shrunk variant, if buffer week allows
